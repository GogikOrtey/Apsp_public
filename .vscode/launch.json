{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run Current File (No Debug)",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            // "noDebug": true
        }
    ]
}
