# Unified Python dependencies for the APSP repo.
#
# Why this file exists:
# - The repo previously had multiple requirements files in subfolders.
# - On Debian/Ubuntu (PEP 668), installing into system Python via pip is blocked.
# - The intended workflow is: create `.venv` and run `pip install -r requirements.txt`.
#
# Web UI (Apsp_front)
Flask==3.0.0
Werkzeug==3.0.1

# Core libs used across scripts
numpy>=1.23.5,<2.0.0
requests==2.31.0

# HTML parsing / selectors (used by `import_all_libraries.py`, `extracting_selector_from_html.py`, etc.)
beautifulsoup4==4.12.3
lxml==4.9.3
parsel==1.10.0
python-dotenv==1.2.1
jsbeautifier

# OpenAI client (requested)
openai


